# agentic-pr-reviewer
An AI PR reviewer that investigates code using tools (semantic search, dependency graphs) before generating review comments, instead of sending the raw diff straight to a model.
